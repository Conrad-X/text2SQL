[
    {
        "id": 1,
        "masked_question": "<unk> all <mask> performing at the <unk> <mask> .",
        "masked_answer": "SELECT <mask>.<mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> WHERE <mask>.<mask> = <unk>;"
    },
    {
        "id": 2,
        "masked_question": "Get the <unk> <unk> of <mask> sold for each <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, COUNT(<mask>.<mask>) AS <mask> FROM <mask> <mask> LEFT JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask>;"
    },
    {
        "id": 3,
        "masked_question": "Find all <unk> <mask> who purchased <mask> before <unk> 1st , <unk> .",
        "masked_answer": "SELECT * FROM <mask> WHERE <mask> = <unk> AND <mask> < <unk>;"
    },
    {
        "id": 4,
        "masked_question": "List the <unk> <unk> most <unk> <mask> based on the <unk> of <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, COUNT(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> ORDER <mask> <mask> <mask> LIMIT <mask>;"
    },
    {
        "id": 5,
        "masked_question": "Retrieve the <mask> from <mask> with a <mask> of <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, <mask>.<mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> WHERE <mask>.<mask> = <mask>;"
    },
    {
        "id": 6,
        "masked_question": "Get the <unk> <mask> <mask> for each <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, SUM(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask>;"
    },
    {
        "id": 7,
        "masked_question": "<unk> all <mask> <mask> for the <unk> <unk> <unk> .",
        "masked_answer": "SELECT * FROM <mask> WHERE <mask> <mask> <mask>() AND <mask>() + <mask> <unk>;"
    },
    {
        "id": 8,
        "masked_question": "Find all <mask> in the <unk> <mask> .",
        "masked_answer": "SELECT * FROM <mask> WHERE <mask> = <unk>;"
    },
    {
        "id": 9,
        "masked_question": "<unk> all <mask> with a <mask> greater than 20,000 .",
        "masked_answer": "SELECT * FROM <mask> WHERE <mask> > <mask>;"
    },
    {
        "id": 10,
        "masked_question": "<unk> all <mask> for <mask> that have already occurred .",
        "masked_answer": "SELECT * FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> WHERE <mask>.<mask> < <mask>();"
    },
    {
        "id": 11,
        "masked_question": "Find the <unk> <mask> <mask> <mask> for each <mask> who <mask> had more than <unk> <mask> . <unk> by <unk> <mask> in descending <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, SUM(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING COUNT(<mask>.<mask>) > <mask> ORDER <mask> <mask> <mask>;"
    },
    {
        "id": 12,
        "masked_question": "List the <unk> <unk> <mask> by <unk> <mask> <mask> , where the <unk> of <mask> <mask> greater than <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, SUM(<mask>.<mask>) AS <mask>, COUNT(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING COUNT(<mask>.<mask>) > <mask> ORDER <mask> <mask> <mask> LIMIT <mask>;"
    },
    {
        "id": 13,
        "masked_question": "<unk> all <mask> who have spent more than $ <unk> on <mask> , and attended at least <unk> <unk> <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, SUM(<mask>.<mask>) AS <mask>, COUNT(DISTINCT <mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING SUM(<mask>.<mask>) > <mask> AND COUNT(DISTINCT <mask>.<mask>) >= <mask>;"
    },
    {
        "id": 14,
        "masked_question": "Find the <unk> <mask> for each <mask> that <mask> received more than <unk> <mask> <mask> , ordered by highest <unk> <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, AVG(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING COUNT(<mask>.<mask>) > <mask> ORDER <mask> <mask> <mask>;"
    },
    {
        "id": 15,
        "masked_question": "Get the <unk> <unk> of <mask> sold and <unk> <mask> for each <mask> for <mask> in the <unk> <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, COUNT(<mask>.<mask>) AS <mask>, SUM(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> WHERE <mask>.<mask> <mask> <mask>() - <mask> <unk> AND <mask>() GROUP <mask> <mask>.<mask>;"
    },
    {
        "id": 16,
        "masked_question": "<unk> all <mask> where more than <unk> <mask> were sold and the <unk> <mask> <unk> exceeds $ 10,000 , ordered by <unk> <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, COUNT(<mask>.<mask>) AS <mask>, SUM(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING COUNT(<mask>.<mask>) > <mask> AND SUM(<mask>.<mask>) > <mask> ORDER <mask> <mask> <mask>;"
    },
    {
        "id": 17,
        "masked_question": "Find all <mask> who provided <mask> for more than <unk> <mask> and gave an <unk> <mask> of at least <unk> .",
        "masked_answer": "SELECT <mask>.<mask>, AVG(<mask>.<mask>) AS <mask>, COUNT(DISTINCT <mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING COUNT(DISTINCT <mask>.<mask>) > <mask> AND AVG(<mask>.<mask>) >= <mask>;"
    },
    {
        "id": 18,
        "masked_question": "Find all <mask> who have made more than $ <unk> in <mask> and had at least <unk> <unk> . List the <mask> and their <unk> <mask> .",
        "masked_answer": "SELECT <mask>.<mask>, SUM(<mask>.<mask>) AS <mask>, COUNT(<mask>.<mask>) AS <mask> FROM <mask> <mask> JOIN <mask> <mask> ON <mask>.<mask> = <mask>.<mask> GROUP <mask> <mask>.<mask> HAVING SUM(<mask>.<mask>) > <mask> AND COUNT(<mask>.<mask>) >= <mask>;"
    }
]